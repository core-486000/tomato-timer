extends layout

block content
  div
    if user
      a(href="/logout") #{user.username} をログアウト 
    else 
      a(href="/login") ログイン
  div#timer-divided
    p#timer 25:00
    button#start-timer-button(type="button") 開始